name: Daily Rebuild

on:
  schedule:
    # 6:00 AM Eastern (11:00 UTC)
    - cron: '0 11 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cloudflare Pages rebuild
        run: curl -X POST ${{ secrets.CF_DEPLOY_HOOK }}
